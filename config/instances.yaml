---
containers: []
virtualmachines:
  - name: "vscode"
    image: "rhel-9.6"
    memory: "4G"
    cores: 1
    image_size: "40G"
    tags:
      - key: "AnsibleGroup"
        value: "bastions"
    networks:
      - default
    services:
      - name: vscode
        ports:
          - port: 3000
            protocol: TCP
            targetPort: 3000
            name: vscode
    routes:
      - name: vscode
        host: vscode
        service: vscode
        targetPort: 3000
        tls: true
        tls_termination: Edge
