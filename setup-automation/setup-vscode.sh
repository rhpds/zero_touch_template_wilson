#!/bin/bash

echo "$(date): VS Code Server setup initiated"
echo "This system uses Ansible playbook for VS Code Server installation"
echo "See: setup-vscode-playbook.yml for the actual installation logic"
echo "Services and routes are configured in: ../config/instances.yaml"

# Exit successfully to indicate the setup is handled elsewhere
exit 0